# Local environment — contains secrets, never commit
.env

# Claude Code local settings
.claude/

# Let's Encrypt certificates (stored in Docker volumes, not the repo)
data/

# Backup archives
backups/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
